Vue d'ensemble — schéma ASCII de l'architecture globale
Mécanisme de masquage du shell SF — les deux techniques combinées :
Injection CSS dans document.head (hors Shadow DOM) avec display: none !important sur les sélecteurs SLDS internes
position: fixed avec z-index: 9999 pour couvrir tout le viewport
Pourquoi 100vh et height: 100% ne fonctionnent pas dans le contexte SF
Arbre des composants — hiérarchie complète
Rôle de chaque composant — API, responsabilités, sécurité (VfContainer), optimisations (DataTable)
Métadonnées Salesforce — App, FlexiPage, Tab, VF Pages
Stratégie Flexbox — flex-shrink: 0 header/footer, flex: 1 1 0% + min-height: 0 body
Flux de données — diagramme des interactions entre composants
Inventaire — 24 fichiers, 5 composants LWC
